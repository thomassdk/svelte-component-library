// Progress Bar
export enum Status {
  InActive,
  Active,
  Success,
  Error,
}

// Identifies the element that provides the accessible name of the meter.
export enum Label {
  Downloading,
  Uploading,
  DataProcessing,
}
